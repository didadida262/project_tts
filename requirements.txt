# Coqui TTS 声音模型训练项目依赖

# 核心TTS框架（需要Python 3.9-3.11）
TTS>=0.20.0

# PyTorch - GPU版本（CUDA）
# 注意：如需使用GPU训练，请先安装对应CUDA版本的PyTorch
# CUDA 11.8: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
# 默认安装CUDA 12.1版本（如果系统不支持会自动降级）

# 音频处理
librosa>=0.9.0
soundfile>=0.12.0
numpy>=1.21.0
pydub>=0.25.1  # 用于处理m4a等格式

# 配置和工具
pyyaml>=6.0
tqdm>=4.65.0
pandas>=1.5.0

# 音频转文本（可选，用于自动生成转录）
openai-whisper>=20231117

